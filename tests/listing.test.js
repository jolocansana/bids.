const listingController = require('../controllers/listingConrtoller')

test('Add Bid To Listing - ', () => {

});

describe('Add Bid To Listing', () => {
    it('When input is not a string, prompt "must be a string"', () => {
    
    })
    
});